server:
  port: 8848

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/CitrusMaxima/mmall-config-server
          search-paths: mmall-configuration/
  application:
    name: mmall-config-server